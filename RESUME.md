## Token 的 key 为什么是固定的？

**面试官：** 你的 `TOKEN_KEY = 'token'` 为什么是固定的？实际开发也这样写吗？

**我：** `token` 只是 localStorage 里的存储名称，类似一个固定的标签；真正的 Token 值是后端在登录成功后动态返回的。学习项目这样写没问题，安全要求高的项目一般会使用 HttpOnly Cookie。

## Token 过期后怎么处理？

**面试官：** 如果 Token 过期了，你的项目会怎么处理？

**我：** 我在 Axios 响应拦截器里统一处理。这个后端会返回业务码 `A00004`，我也兼容了 HTTP 401。发现 Token 失效后就清除本地 Token，记录当前页面并跳转登录页，重新登录后还能回到原页面。

## 搜索联想为什么使用防抖？

**面试官：** 你的搜索防抖是怎么实现的？

**我：** 我在 AI 搜索联想里做了 300 毫秒防抖。每次输入时先清除上一次定时器，用户停止输入 300 毫秒后才发送请求，避免每输入一个字都调用一次接口。搜索、清空输入或离开页面时，我也会清除定时器。

## 项目中最有技术挑战的功能是什么？

**面试官：** 你能不能从这些项目里面，选一个你觉得最能体现你的技术能力的，或者说最有技术挑战的一个小点，跟我们稍微分享一下？

**我：** 我想分享一下轻购项目里的 AI 搜索联想。这个功能表面上只是调用接口，但实际还要处理频繁请求、返回顺序混乱、结果格式不稳定和服务异常等问题。

我先加了 300 毫秒防抖，减少用户连续输入产生的无效请求。用户输入新内容时，我会通过 `AbortController` 取消上一次请求，并给每次请求设置编号，只允许最后一次请求更新页面，避免旧结果覆盖新结果。相同关键词五分钟内会直接读取缓存，减少重复请求。

接口返回后，我还会清理编号和多余字符，限制每条内容的长度，并检查是否正好返回五条。如果接口超时、报错或者格式不正确，就切换到本地推荐，保证这个功能仍然可以使用。请求通过服务端函数转发，密钥不会暴露在前端。

这个功能让我认识到，接入第三方服务不只是把接口调通，还要考虑请求频率、并发顺序、数据格式、安全和异常降级。
